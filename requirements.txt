# Qwen 2.5 32B 双专家 QLoRA 推理依赖
# 适用于 V100 GPU

# 模型下载
modelscope>=1.11.0
huggingface_hub>=0.20.0

# 推理引擎
vllm>=0.6.0
torch>=2.0.0
transformers>=4.37.0

# GPTQ 支持 (vLLM 内置，部分模型可能需要)
# auto-gptq  # 如使用 transformers 直接加载 GPTQ 时取消注释
