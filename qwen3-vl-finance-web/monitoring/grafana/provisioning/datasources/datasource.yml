apiVersion: 1
datasources:
  # Prometheus 使用 host 网络时监听在主机 9090，Grafana 在容器内通过 host.docker.internal 访问
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://host.docker.internal:9090
    isDefault: true
